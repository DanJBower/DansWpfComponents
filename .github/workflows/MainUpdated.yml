name: Main Branch Updated

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  CallBuildNuGet:
    uses: ./.github/workflows/BuildNuGet.yml

  CallReleaseNuGetToPrivateFeed:
    needs: CallBuildNuGet
    uses: ./.github/workflows/ReleaseNuGetToPrivateFeed.yml
